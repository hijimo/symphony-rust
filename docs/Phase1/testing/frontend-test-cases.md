# 前端测试用例清单

## 覆盖率目标

| 模块 | 行覆盖率 | 分支覆盖率 |
|------|---------|-----------|
| stores/ | 90% | 85% |
| utils/ | 90% | 85% |
| components/ | 80% | 75% |
| pages/ | 75% | 70% |
| hooks/ | 85% | 80% |
| 整体 | 80% | 75% |

---

## 1. 登录页 (LoginPage)

### 1.1 组件渲染测试

| # | 用例名称 | 前置条件 | 步骤 | 预期结果 |
|---|---------|---------|------|---------|
| 1 | 渲染登录表单 | 无 | 渲染 LoginPage | 显示用户名输入框、密码输入框、登录按钮 |
| 2 | 密码输入框默认隐藏 | 无 | 渲染 LoginPage | 密码输入框 type=password |
| 3 | 密码可见性切换 | 无 | 点击密码可见性图标 | type 切换为 text |
| 4 | 登录按钮初始可用 | 无 | 渲染 LoginPage | 登录按钮 enabled |
| 5 | 已登录用户重定向 | localStorage 有有效 token | 渲染 LoginPage | 重定向到 /dashboard |

### 1.2 表单验证测试

| # | 用例名称 | 前置条件 | 步骤 | 预期结果 |
|---|---------|---------|------|---------|
| 6 | 空用户名提交 | 无 | 密码填写，用户名为空，点击登录 | 显示"请输入用户名"错误 |
| 7 | 空密码提交 | 无 | 用户名填写，密码为空，点击登录 | 显示"请输入密码"错误 |
| 8 | 全空提交 | 无 | 直接点击登录 | 显示两个字段的错误提示 |
| 9 | 输入后清除错误 | 已显示错误 | 在对应输入框输入内容 | 错误提示消失 |

### 1.3 登录交互测试

| # | 用例名称 | 前置条件 | 步骤 | 预期结果 |
|---|---------|---------|------|---------|
| 10 | 登录成功跳转 | MSW 返回 200 | 输入正确凭据，点击登录 | 跳转到 /dashboard |
| 11 | 登录成功存储 token | MSW 返回 200 | 输入正确凭据，点击登录 | localStorage 存储 token |
| 12 | 登录失败显示错误 | MSW 返回 401 | 输入错误凭据，点击登录 | 显示"用户名或密码错误" |
| 13 | 登录中禁用按钮 | 无 | 点击登录后 | 按钮显示 loading 状态，不可重复点击 |
| 14 | 网络错误提示 | MSW 返回 500 | 点击登录 | 显示"服务器错误，请稍后重试" |
| 15 | Rate limit 提示 | MSW 返回 429 | 点击登录 | 显示"请求过于频繁，请稍后重试" |
| 16 | Enter 键提交 | 焦点在密码框 | 按 Enter 键 | 触发登录 |

---

## 2. 用户管理页 (UserManagementPage)

### 2.1 页面渲染测试

| # | 用例名称 | 前置条件 | 步骤 | 预期结果 |
|---|---------|---------|------|---------|
| 17 | 渲染用户列表 | admin 已登录，MSW 返回用户列表 | 渲染页面 | 显示用户表格，含用户名、角色、创建时间列 |
| 18 | 显示加载状态 | admin 已登录 | 渲染页面（API 延迟） | 显示 loading skeleton |
| 19 | 显示空状态 | admin 已登录，MSW 返回空列表 | 渲染页面 | 显示"暂无用户"提示 |
| 20 | 显示错误状态 | admin 已登录，MSW 返回 500 | 渲染页面 | 显示错误提示和重试按钮 |
| 21 | 非 admin 无法访问 | user 角色已登录 | 访问 /admin/users | 重定向到 /dashboard |

### 2.2 创建用户测试

| # | 用例名称 | 前置条件 | 步骤 | 预期结果 |
|---|---------|---------|------|---------|
| 22 | 打开创建对话框 | 页面已渲染 | 点击"添加用户"按钮 | 弹出创建用户对话框 |
| 23 | 创建用户成功 | 对话框已打开 | 填写有效信息，点击确认 | 对话框关闭，列表刷新，显示成功提示 |
| 24 | 创建用户 - 用户名为空 | 对话框已打开 | 用户名为空，点击确认 | 显示验证错误 |
| 25 | 创建用户 - 密码不合规 | 对话框已打开 | 密码为 "123"，点击确认 | 显示密码规则提示 |
| 26 | 创建用户 - 用户名重复 | 对话框已打开，MSW 返回 409 | 填写已存在用户名，点击确认 | 显示"用户名已存在" |
| 27 | 取消创建 | 对话框已打开 | 点击取消 | 对话框关闭，无 API 调用 |

### 2.3 删除用户测试

| # | 用例名称 | 前置条件 | 步骤 | 预期结果 |
|---|---------|---------|------|---------|
| 28 | 点击删除弹出确认 | 列表有用户 | 点击用户行的删除按钮 | 弹出确认对话框 |
| 29 | 确认删除成功 | 确认对话框已打开 | 点击确认 | 用户从列表消失，显示成功提示 |
| 30 | 取消删除 | 确认对话框已打开 | 点击取消 | 对话框关闭，用户仍在列表 |
| 31 | 删除失败提示 | 确认对话框已打开，MSW 返回 500 | 点击确认 | 显示错误提示 |
| 32 | 不能删除自己 | admin 查看列表 | 自己的行无删除按钮 | 删除按钮不显示或禁用 |

### 2.4 重置密码测试

| # | 用例名称 | 前置条件 | 步骤 | 预期结果 |
|---|---------|---------|------|---------|
| 33 | 重置密码成功 | 列表有用户 | 点击重置密码 → 确认 | 显示临时密码（可复制） |
| 34 | 重置密码取消 | 确认对话框已打开 | 点击取消 | 对话框关闭 |
| 35 | 复制临时密码 | 临时密码已显示 | 点击复制按钮 | 复制到剪贴板，显示"已复制" |

---

## 3. 个人设置页 (SettingsPage)

### 3.1 Profile 标签页

| # | 用例名称 | 前置条件 | 步骤 | 预期结果 |
|---|---------|---------|------|---------|
| 36 | 渲染个人信息 | 已登录 | 渲染设置页 | 显示当前用户名（只读）、显示名称（可编辑） |
| 37 | 修改显示名称 | 已登录 | 修改 display_name，点击保存 | 显示成功提示，值更新 |
| 38 | 显示名称为空 | 已登录 | 清空 display_name，点击保存 | 显示验证错误 |
| 39 | 未修改时保存按钮禁用 | 已登录 | 不做任何修改 | 保存按钮 disabled |

### 3.2 密码修改标签页

| # | 用例名称 | 前置条件 | 步骤 | 预期结果 |
|---|---------|---------|------|---------|
| 40 | 渲染密码修改表单 | 已登录 | 切换到密码标签 | 显示旧密码、新密码、确认密码输入框 |
| 41 | 修改密码成功 | 已登录 | 填写正确旧密码和合规新密码 | 显示成功，跳转到登录页 |
| 42 | 旧密码错误 | 已登录，MSW 返回 401 | 填写错误旧密码 | 显示"旧密码错误" |
| 43 | 新密码不合规 | 已登录 | 新密码为 "123" | 显示密码规则提示 |
| 44 | 两次密码不一致 | 已登录 | 新密码和确认密码不同 | 显示"两次密码不一致" |
| 45 | 新旧密码相同 | 已登录 | 新密码等于旧密码 | 显示"新密码不能与旧密码相同" |

### 3.3 Token 配置标签页

| # | 用例名称 | 前置条件 | 步骤 | 预期结果 |
|---|---------|---------|------|---------|
| 46 | 渲染 Token 配置 | 已登录 | 切换到 Token 标签 | 显示 GitLab Token、GitLab Host、GitHub Token 输入框 |
| 47 | Token 显示为掩码 | 已有 token 配置 | 渲染页面 | Token 显示为 ******* |
| 48 | 保存 Token 成功 | 已登录 | 填写 token，点击保存 | 显示成功提示 |
| 49 | GitLab Host 格式验证 | 已登录 | 输入 "not-a-url" | 显示"请输入有效的 URL" |
| 50 | 清空 Token | 已有 token | 清空 token 字段，点击保存 | 成功清除 |

---

## 4. 路由守卫测试

| # | 用例名称 | 前置条件 | 步骤 | 预期结果 |
|---|---------|---------|------|---------|
| 51 | 未登录访问受保护页面 | 无 token | 访问 /dashboard | 重定向到 /login |
| 52 | 未登录访问登录页 | 无 token | 访问 /login | 正常显示登录页 |
| 53 | 已登录访问登录页 | 有有效 token | 访问 /login | 重定向到 /dashboard |
| 54 | token 过期后访问 | token 已过期 | 访问 /dashboard | 重定向到 /login |
| 55 | user 访问 admin 路由 | user 角色 token | 访问 /admin/users | 重定向到 /dashboard 或显示 403 |
| 56 | admin 访问 admin 路由 | admin 角色 token | 访问 /admin/users | 正常显示 |
| 57 | 登出后访问受保护页面 | 刚执行登出 | 访问 /dashboard | 重定向到 /login |

---

## 5. Store 逻辑测试

### 5.1 AuthStore

| # | 用例名称 | 前置条件 | 步骤 | 预期结果 |
|---|---------|---------|------|---------|
| 58 | 初始状态 | 无 token | 读取 store | isAuthenticated=false, user=null |
| 59 | 从 localStorage 恢复 | 有有效 token | 初始化 store | isAuthenticated=true |
| 60 | login 成功更新状态 | 无 | 调用 login(credentials) | isAuthenticated=true, user 有值 |
| 61 | login 失败不更新状态 | 无 | 调用 login(bad_credentials) | isAuthenticated=false, error 有值 |
| 62 | logout 清除状态 | 已登录 | 调用 logout() | isAuthenticated=false, token 清除 |
| 63 | token 过期自动登出 | token 即将过期 | API 返回 401 | 自动调用 logout |

### 5.2 UserStore (Admin)

| # | 用例名称 | 前置条件 | 步骤 | 预期结果 |
|---|---------|---------|------|---------|
| 64 | fetchUsers 成功 | admin 已登录 | 调用 fetchUsers() | users 列表更新 |
| 65 | fetchUsers 失败 | admin 已登录 | API 返回 500 | error 状态更新 |
| 66 | createUser 成功 | admin 已登录 | 调用 createUser(data) | 列表新增用户 |
| 67 | deleteUser 成功 | admin 已登录 | 调用 deleteUser(id) | 列表移除用户 |
| 68 | deleteUser 乐观更新回滚 | admin 已登录 | 调用 deleteUser(id)，API 失败 | 用户恢复到列表 |

---

## 6. 工具函数测试

### 6.1 验证器 (validators)

| # | 用例名称 | 输入 | 预期结果 |
|---|---------|------|---------|
| 69 | validateUsername - 有效 | "john_doe" | { valid: true } |
| 70 | validateUsername - 空 | "" | { valid: false, error: "required" } |
| 71 | validateUsername - 太短 | "ab" | { valid: false, error: "too_short" } |
| 72 | validateUsername - 特殊字符 | "user@name" | { valid: false, error: "invalid_chars" } |
| 73 | validatePassword - 有效 | "Admin@123456" | { valid: true } |
| 74 | validatePassword - 太短 | "Ab@1" | { valid: false, error: "too_short" } |
| 75 | validatePassword - 无大写 | "admin@123" | { valid: false, error: "no_uppercase" } |
| 76 | validatePassword - 无特殊字符 | "Admin1234" | { valid: false, error: "no_special" } |
| 77 | validateUrl - 有效 HTTPS | "https://gitlab.com" | { valid: true } |
| 78 | validateUrl - 无协议 | "gitlab.com" | { valid: false, error: "invalid_url" } |
| 79 | validateUrl - HTTP | "http://gitlab.com" | { valid: true }（允许内网） |

### 6.2 API 工具

| # | 用例名称 | 前置条件 | 步骤 | 预期结果 |
|---|---------|---------|------|---------|
| 80 | 请求自动附加 token | localStorage 有 token | 发送 GET 请求 | Authorization header 正确 |
| 81 | 401 响应清除 token | 有 token | API 返回 401 | localStorage token 清除 |
| 82 | 401 响应触发重定向 | 有 token | API 返回 401 | 跳转到 /login |
| 83 | 请求超时处理 | 无 | API 超时 | 显示超时错误 |
| 84 | 请求重试机制 | 无 | API 返回 503 | 自动重试（最多 3 次） |

---

## 7. E2E 测试 (Playwright)

### 7.1 登录流程

| # | 用例名称 | 前置条件 | 步骤 | 预期结果 |
|---|---------|---------|------|---------|
| 85 | 完整登录流程 | 后端运行 | 1. 打开 /login<br>2. 输入 admin/Admin@123456<br>3. 点击登录 | 跳转到 /dashboard |
| 86 | 登录失败流程 | 后端运行 | 1. 打开 /login<br>2. 输入错误密码<br>3. 点击登录 | 显示错误提示，停留在登录页 |
| 87 | 登出流程 | 已登录 | 1. 点击用户头像<br>2. 点击退出 | 跳转到 /login，token 清除 |

### 7.2 用户管理流程

| # | 用例名称 | 前置条件 | 步骤 | 预期结果 |
|---|---------|---------|------|---------|
| 88 | 创建用户完整流程 | admin 已登录 | 1. 导航到用户管理<br>2. 点击添加<br>3. 填写信息<br>4. 确认 | 新用户出现在列表 |
| 89 | 删除用户完整流程 | admin 已登录，有多个用户 | 1. 导航到用户管理<br>2. 点击删除<br>3. 确认 | 用户从列表消失 |
| 90 | 重置密码完整流程 | admin 已登录 | 1. 点击重置密码<br>2. 确认<br>3. 复制临时密码 | 临时密码可复制 |

### 7.3 个人设置流程

| # | 用例名称 | 前置条件 | 步骤 | 预期结果 |
|---|---------|---------|------|---------|
| 91 | 修改显示名称 | 已登录 | 1. 导航到设置<br>2. 修改名称<br>3. 保存 | 名称更新，顶栏显示新名称 |
| 92 | 修改密码后重新登录 | 已登录 | 1. 修改密码<br>2. 自动跳转登录<br>3. 用新密码登录 | 登录成功 |
| 93 | 配置 Token | 已登录 | 1. 切换到 Token 标签<br>2. 输入 token<br>3. 保存 | 保存成功，token 显示为掩码 |

### 7.4 跨页面导航

| # | 用例名称 | 前置条件 | 步骤 | 预期结果 |
|---|---------|---------|------|---------|
| 94 | 侧边栏导航 | admin 已登录 | 依次点击各菜单项 | 正确切换页面，URL 更新 |
| 95 | 浏览器后退 | 已导航多个页面 | 点击浏览器后退 | 返回上一页面 |
| 96 | 直接 URL 访问 | admin 已登录 | 直接输入 /admin/users | 正确渲染页面 |
| 97 | 404 页面 | 已登录 | 访问不存在的路由 | 显示 404 页面 |

### 7.5 响应式布局

| # | 用例名称 | 前置条件 | 步骤 | 预期结果 |
|---|---------|---------|------|---------|
| 98 | 移动端登录页 | viewport 375x667 | 渲染登录页 | 表单正常显示，无溢出 |
| 99 | 平板端用户列表 | viewport 768x1024 | 渲染用户管理页 | 表格自适应，操作按钮可见 |
| 100 | 桌面端完整布局 | viewport 1920x1080 | 渲染任意页面 | 侧边栏 + 内容区正常布局 |
